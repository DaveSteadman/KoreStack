# KoreChat

KoreChat is the suite conversation-state service.

The live implementation now lives directly in this folder.

Run it with:

```python
python main.py
```

The legacy `KoreAgent/code/KoreChat/` location is now only a compatibility
shim for older launch paths.