# UIElements

UIElements is the shared UI library layer for the Kore suite.

It is now the canonical home for the shared shell assets, templates, tokens, and UI
conventions that used to live under `KoreCommonUI`.

UIElements is not a process. It is a shared library of assets, templates, tokens, and UI
shell behavior used by the runnable Kore services.