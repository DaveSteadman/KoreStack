/**
 * @deprecated Import from /ui-elements/assets/js/chrome.js directly.
 *
 * This compatibility module intentionally owns no implementation so legacy
 * URLs receive the same app menu behaviour as every other suite application.
 */
export {
  configureAppMenu,
  initAppMenuEvents,
  renderAppMenu,
} from '/ui-elements/assets/js/chrome.js';
