[
  {
    "exchange": "worker_chat_scratchpad_result_contract",
    "turns": [
      {
        "user": "Use chat_spawn to create an isolated worker chat. Do not calculate the answer yourself. Its worker prompt must use python_execute to calculate 17 + 25 and return only the number. Set tools_allowlist to exactly [\"python_execute\"], result_target to \"scratchpad:worker_chat_sum\", result_format to \"plain text\", and max_iterations to 3. Reply only with the returned worker chat id.",
        "assert": "contains|chat_"
      },
      {
        "user": "Use chat_status for the worker chat created in this exchange. Reply only with its lifecycle status.",
        "assert": "regex|queued|running|completed|failed|cancelled|timed_out"
      },
      {
        "user": "Use chat_result for the worker chat created in this exchange. Reply only with result.summary.",
        "assert": "contains|42"
      },
      {
        "user": "Load scratchpad key worker_chat_sum and reply only with its exact value.",
        "assert": "contains|42"
      }
    ]
  },
  {
    "exchange": "worker_chat_json_dataset_contract",
    "turns": [
      {
        "user": "Use chat_spawn to create an isolated worker chat. Do not create the dataset yourself. Its worker prompt must use python_execute and return valid JSON only: [{\"number\":2,\"square\":4},{\"number\":3,\"square\":9}]. Set tools_allowlist to exactly [\"python_execute\"], result_target to \"dataset:worker_chat_squares\", result_format to \"JSON array of objects\", and max_iterations to 3. Reply only with the returned worker chat id.",
        "assert": "contains|chat_"
      },
      {
        "user": "Use chat_result for the worker chat created in this exchange. Reply with a short confirmation that includes worker_chat_squares.",
        "assert": "contains|worker_chat_squares"
      },
      {
        "user": "Inspect dataset worker_chat_squares and reply with its record count and the field names.",
        "assert": "all_contains|2||number||square"
      }
    ]
  }
]
