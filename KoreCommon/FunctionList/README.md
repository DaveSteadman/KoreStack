# Function List Generator

This directory contains a script to automatically generate a Markdown list of all Python functions in the workspace.

## Usage

To update the `FunctionList.md` file, run the following command from the workspace root:

```bash
python KoreCommon/FunctionList/generate_function_list.py
```

The generated `FunctionList.md` will be located in this directory.
