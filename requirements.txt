# KoreStack — consolidated Python dependencies
#
# Single virtual environment for the entire suite (Windows):
#   python -m venv .venv
#   .venv\Scripts\Activate.ps1
#   pip install -r requirements.txt
#
# Linux:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt


# ── Core web framework (all services) ─────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29.0
python-dotenv>=1.0

# ── HTTP / templates ──────────────────────────────────────────────────────
httpx>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9

# ── Web scraping / content extraction ────────────────────────────────────
beautifulsoup4>=4.12.0
trafilatura>=1.9.0
markdownify>=0.12.0
markdown>=3.6
feedparser>=6.0.0

# ── Scheduling ────────────────────────────────────────────────────────────
apscheduler>=3.10.4

# ── System metrics ────────────────────────────────────────────────────────
psutil>=5.9
certifi>=2024.0

# ── Google / Auth (KoreComms) ─────────────────────────────────────────────
cryptography>=42.0.0
paramiko>=5.0.0
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.126.0
chromadb>=1.0
