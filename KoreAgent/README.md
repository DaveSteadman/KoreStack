# KoreAgent

KoreAgent is the process-level name for the suite's main agent runtime.

The executable entrypoint and the implementation tree now both live inside this folder.

The main runtime launcher is `main.py`, and the internal code lives under `code/`.