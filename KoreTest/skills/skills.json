{
  "schema_version": 1,
  "service": "koretest",
  "service_label": "KoreTest",
  "skills": [
    {
      "name": "test_list_suites",
      "purpose": "List the available KoreTest prompt suites.",
      "keywords": [
        "testing",
        "test",
        "suite",
        "list"
      ],
      "parameters": {
        "type": "object",
        "properties": {}
      },
      "returns": "Available prompt-suite file names.",
      "invoke_path": "/api/skills/test_list_suites/invoke",
      "selection_description": "List the available KoreTest prompt suites."
    },
    {
      "name": "test_run_suite",
      "purpose": "Run one KoreTest prompt suite, optionally with a specified model.",
      "keywords": [
        "testing",
        "test",
        "suite",
        "run",
        "evaluation"
      ],
      "parameters": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "model": {
            "type": "string",
            "default": ""
          }
        },
        "required": [
          "name"
        ]
      },
      "returns": "The started test-run record.",
      "invoke_path": "/api/skills/test_run_suite/invoke",
      "selection_description": "Run one KoreTest prompt suite, optionally with a specified model."
    },
    {
      "name": "test_list_runs",
      "purpose": "List recent KoreTest test-run records.",
      "keywords": [
        "testing",
        "test",
        "results",
        "runs",
        "list"
      ],
      "parameters": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "default": 50
          }
        }
      },
      "returns": "Recent test-run records.",
      "invoke_path": "/api/skills/test_list_runs/invoke",
      "selection_description": "List recent KoreTest test-run records."
    },
    {
      "name": "test_get_trend",
      "purpose": "Read KoreTest pass-rate and token-use trends for a suite.",
      "keywords": [
        "testing",
        "test",
        "results",
        "trend",
        "evaluation"
      ],
      "parameters": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "default": ""
          }
        }
      },
      "returns": "Trend data for test results.",
      "invoke_path": "/api/skills/test_get_trend/invoke",
      "selection_description": "Read KoreTest pass-rate and token-use trends for a suite."
    }
  ]
}
